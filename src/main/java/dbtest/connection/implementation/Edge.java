package dbtest.connection.implementation;

/**
 * Since edges currently don't need any values, we can use an empty object for
 * them.
 *
 * We still use a dedicated empty object instead of just Object.class, for the
 * case that we want to change that later on.
 */
public class Edge
{
}
